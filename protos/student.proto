syntax = "proto3";
package student;

// This should be referencing the "person.proto" sibling file in this directory.
import "deps/person.proto";

message Student {
    person.Person person = 1;
    uint32 yearOfSchool = 2;
}